/**
 * 基础路径
 */
export enum PageEnum {
  /**
   * 基本登录路径
   */
  BASE_LOGIN = '/login',
  /**
   * 基本路径
   */
  BASE_HOME = '/home',
  /**
   * 错误页面路径
   */
  ERROR_PAGE = '/exception',
  /**
   * /错误日志页面路径
   */
  ERROR_LOG_PAGE = '/exception/error-log',
}
